{
  "name": "e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end smoke net — boots Postgres, Nest and a Next build, and drives them with Playwright",
  "scripts": {
    "pretest:e2e": "node scripts/prepare.mjs",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@foodnote/shared": "*",
    "@playwright/test": "^1.50.0",
    "@types/node": "^20",
    "pg": "^8.13.1",
    "typescript": "^5"
  }
}
