# Where Nest lives. Required — every call to the backend is server-to-server
# now, so there is no default to fall back on, and a missing value fails the
# build rather than the first request. Copy this file to `.env.local`.
API_URL=http://localhost:3001
